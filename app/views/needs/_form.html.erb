<%= bootstrap_form_for(@need) do |f| %>
  <% if @need.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@need.errors.count, "error") %> prohibited this need from being saved:</h2>

      <ul>
      <% @need.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.text_field :info, label: "Tiedot", placeholder: "Kirjoita tiedot" %>
    <%= f.select :cabin_id, options_from_collection_for_select(@cabins, :id, :name, selected: @need.cabin_id),label: "Valitse mökki" %>
    <%= f.form_group do %>
        <%= f.submit "Luo puute" %>
    <% end %>
<% end %>
