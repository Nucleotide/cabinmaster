<div id="notice"><%= notice %></div>
<h1>Listing supplies</h1>
<p>You can order the list based on cabin, user or the information.<br/>
    Want to search? Ctrl + F</p>

<table>
  <thead>
    <tr>
      <th><%= link_to 'Cabin', supplies_path(order:"cabin") %></th>
      <th><%= link_to 'User', supplies_path(order:"user") %></th>
      <th><%= link_to 'Info', supplies_path(order:"info") %></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @supplies.each do |supply| %>
      <tr>
        <td><%= supply.cabin_name(supply.cabin_id) %></td>
        <td><%= supply.user_name(supply.user_id) %></td>
        <td><%= supply.info %></td>
        <td><%= link_to 'Edit', edit_supply_path(supply) %></td>
        <td><%= link_to 'Destroy', supply, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<% unless current_user.nil? %>
    <%= link_to 'New Supply', new_supply_path %>
<% end %>