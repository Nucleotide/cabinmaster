<%= bootstrap_form_for(@supply) do |f| %>
  <% if @supply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supply.errors.count, "error") %> prohibited this supply from being saved:</h2>

      <ul>
      <% @supply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :info, label: "Tiedot", placeholder: "Kirjoita tiedot" %>
    <%= f.select :cabin_id, options_from_collection_for_select(@cabins, :id, :name, selected: @supply.cabin_id),label: "Valitse mökki" %>
    <%= f.submit "Tallenna" %>
<% end %>
